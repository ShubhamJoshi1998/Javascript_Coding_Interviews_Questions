// What will be the output of below code snippet:

function greetHello(name) {
  return `Hello, ${name}!`;
}
console.log(greetHello("Brian"));

// Here greetHello function paases an argument name and return Hello with argument.

// At the time of invoking greetHello it passes "Brian" as an argument it will take the value of argument.
